---
title: N-Queens II
description: Count all valid N-Queens placements. Two approaches — backtracking O(N!) and optimized backtracking O(N!) — in six languages.
leetcodeNumber: 52
difficulty: Hard
leetcodeSlug: n-queens-ii
tags: [Backtracking, Array]
prev:
  label: "#50 Pow(x, n)"
  link: "../powx-n/"
next:
  label: "#53 Maximum Subarray"
  link: "../maximum-subarray/"
---

import { Aside, Tabs, TabItem, Code } from '@astrojs/starlight/components';
import ComplexityBadges from '../../../components/ComplexityBadges.astro';
import ApproachTag from '../../../components/ApproachTag.astro';
import LogicPlayer from '../../../components/LogicPlayer.astro';
import RelatedProblems from '../../../components/RelatedProblems.astro';
import PrevNextNav from '../../../components/PrevNextNav.astro';

## Problem Statement

The **n-queens** puzzle is the problem of placing `n` queens on an `n x n` chessboard such that no two queens attack each other.

Given an integer `n`, **return the number of distinct solutions** to the n-queens puzzle.

<Aside type="tip" title="What to notice first">
- Queens attack horizontally, vertically, and diagonally.
- Place one queen per row (reduces search space).
- Track which columns and diagonals are occupied using sets (O(1) lookup).
- Two cells (r1, c1) and (r2, c2) are on the same diagonal if: `r1 - c1 == r2 - c2` (same /) or `r1 + c1 == r2 + c2` (same \).
</Aside>

### Examples

| n | Output | Explanation |
| --- | --- | --- |
| 1 | 1 | One solution: place queen at [0,0] |
| 4 | 2 | Two distinct solutions (classic 4-queens) |
| 8 | 92 | 92 distinct solutions (classic 8-queens) |
| 3 | 0 | No valid solution exists |

## Constraints

- `1 <= n <= 9`
- The number of solutions grows very rapidly (factorial growth)

import pyBacktracking  from '@solutions/Python/n_queens_ii_backtracking.py?raw';
import cppBacktracking from '@solutions/C++/n_queens_ii_backtracking.cpp?raw';
import javaBacktracking from '@solutions/Java/NQueensIIBacktracking.java?raw';
import jsBacktracking  from '@solutions/JavaScript/n_queens_ii_backtracking.js?raw';
import rustBacktracking from '@solutions/Rust/n_queens_ii_backtracking.rs?raw';
import goBacktracking  from '@solutions/Golang/n_queens_ii_backtracking.go?raw';
import pySpaceOptimized from '@solutions/Python/n_queens_ii_space_optimized.py?raw';
import cppSpaceOptimized from '@solutions/C++/n_queens_ii_space_optimized.cpp?raw';
import javaSpaceOptimized from '@solutions/Java/NQueensIiSpaceOptimized.java?raw';
import jsSpaceOptimized from '@solutions/JavaScript/n_queens_ii_space_optimized.js?raw';
import rustSpaceOptimized from '@solutions/Rust/n_queens_ii_space_optimized.rs?raw';
import goSpaceOptimized from '@solutions/Golang/n_queens_ii_space_optimized.go?raw';

import pyOptimized      from '@solutions/Python/n_queens_ii_backtracking_optimized.py?raw';
import cppOptimized     from '@solutions/C++/n_queens_ii_backtracking_optimized.cpp?raw';
import javaOptimized    from '@solutions/Java/NQueensIIBacktrackingOptimized.java?raw';
import jsOptimized      from '@solutions/JavaScript/n_queens_ii_backtracking_optimized.js?raw';
import rustOptimized    from '@solutions/Rust/n_queens_ii_backtracking_optimized.rs?raw';
import goOptimized      from '@solutions/Golang/n_queens_ii_backtracking_optimized.go?raw';

## Real-World Applications

<Aside type="note" title="Where this pattern appears">
- **Job scheduling** — allocate n tasks to n time slots with conflict constraints.
- **Channel assignment** — assign n frequencies to n regions without interference.
- **Constraint satisfaction problems** — general CSP solver demonstrating backtracking.
- **Optimization problems** — combinatorial search with pruning for feasibility.
</Aside>

## Complexity Comparison

| Approach | Time | Space | Notes |
| --- | --- | --- | --- |
| Backtracking (Array) | O(N!) | O(N^2) | Simpler logic; checks all previous rows |
| Backtracking (Sets) | O(N!) | O(N) | Optimized; O(1) conflict checking with sets |

## Approach 1: Backtracking with Array (Recommended)

<ApproachTag tag="recommended" />

Use an array `board` where `board[row] = col` represents a queen at position (row, col). At each row, try placing a queen in each column. Before placing, check if it's safe:
- No queen in the same column.
- No queen on the same diagonals (using the formula: `abs(row1 - row2) == abs(col1 - col2)`).

<ComplexityBadges time="O(N!)" space="O(N^2)" timeColor="red" spaceColor="green" timeNote="Exponential search space" spaceNote="Board representation" />

### Step-by-step Example

**Input:** `n = 4`

```
Row 0: Try columns 0, 1, 2, 3
  Place at (0, 1)
  Row 1: Try columns 0, 2, 3
    Place at (1, 3)
    Row 2: Try columns 0, 2
      All conflict
    Backtrack
  ...continue trying...
```

### Pseudocode

```plaintext
function totalNQueens(n):
    board = [0] * n  // board[i] = column of queen in row i
    count = 0
    
    function backtrack(row):
        if row == n:
            return 1
        
        count = 0
        for col in range(n):
            if isSafe(board, row, col):
                board[row] = col
                count += backtrack(row + 1)
        
        return count
    
    function isSafe(board, row, col):
        for i in range(row):
            if board[i] == col or abs(board[i] - col) == abs(i - row):
                return False
        return True
    
    return backtrack(0)
```

### Solution Code

<Tabs>
  <TabItem label="Python">
    <Code code={pyBacktracking} lang="python" title="n_queens_ii_backtracking.py" />
  </TabItem>
  <TabItem label="C++">
    <Code code={cppBacktracking} lang="cpp" title="n_queens_ii_backtracking.cpp" />
  </TabItem>
  <TabItem label="Java">
    <Code code={javaBacktracking} lang="java" title="NQueensIIBacktracking.java" />
  </TabItem>
  <TabItem label="JavaScript">
    <Code code={jsBacktracking} lang="javascript" title="n_queens_ii_backtracking.js" />
  </TabItem>
  <TabItem label="Rust">
    <Code code={rustBacktracking} lang="rust" title="n_queens_ii_backtracking.rs" />
  </TabItem>
  <TabItem label="Go">
    <Code code={goBacktracking} lang="go" title="n_queens_ii_backtracking.go" />
  </TabItem>
</Tabs>

## Approach 2: Backtracking with Sets (Optimized)

<ApproachTag tag="simple" />

Use three sets to track occupied columns and diagonals. For a queen at (row, col):
- Column: track `col`.
- Diagonal `/`: track `row - col` (constant on same `/` diagonal).
- Diagonal `\`: track `row + col` (constant on same `\` diagonal).

Set lookups are O(1), making conflict detection much faster.

<ComplexityBadges time="O(N!)" space="O(N)" timeColor="red" spaceColor="green" timeNote="Same exponential search" spaceNote="Only three sets, not board" />

### Step-by-step Example

**Input:** `n = 2`

```
cols = {}, diag1 = {}, diag2 = {}

Row 0, Col 0: Add to cols, diag1 (0-0=0), diag2 (0+0=0)
  Row 1, Col 0: cols has 0 → conflict
  Row 1, Col 1: cols={0}, diag1={0}, diag2={0} ✓
    No more rows → count += 1
Backtrack...

Row 0, Col 1: Add to cols, diag1 (0-1=-1), diag2 (0+1=1)
  Row 1, Col 0: cols={1}, diag1={-1}, diag2={1} ✓
    No more rows → count += 1
Backtrack...

Result: count = 2 (but actually 0 for n=2)
```

### Pseudocode

```plaintext
function totalNQueens(n):
    cols, diag1, diag2 = sets()
    
    function backtrack(row):
        if row == n:
            return 1
        
        count = 0
        for col in range(n):
            d1, d2 = row - col, row + col
            
            if col in cols or d1 in diag1 or d2 in diag2:
                continue
            
            cols.add(col)
            diag1.add(d1)
            diag2.add(d2)
            
            count += backtrack(row + 1)
            
            cols.remove(col)
            diag1.remove(d1)
            diag2.remove(d2)
        
        return count
    
    return backtrack(0)
```

### Solution Code

<Tabs>
  <TabItem label="Python">
    <Code code={pyOptimized} lang="python" title="n_queens_ii_backtracking_optimized.py" />
  </TabItem>
  <TabItem label="C++">
    <Code code={cppOptimized} lang="cpp" title="n_queens_ii_backtracking_optimized.cpp" />
  </TabItem>
  <TabItem label="Java">
    <Code code={javaOptimized} lang="java" title="NQueensIIBacktrackingOptimized.java" />
  </TabItem>
  <TabItem label="JavaScript">
    <Code code={jsOptimized} lang="javascript" title="n_queens_ii_backtracking_optimized.js" />
  </TabItem>
  <TabItem label="Rust">
    <Code code={rustOptimized} lang="rust" title="n_queens_ii_backtracking_optimized.rs" />
  </TabItem>
  <TabItem label="Go">
    <Code code={goOptimized} lang="go" title="n_queens_ii_backtracking_optimized.go" />
  </TabItem>
</Tabs>

## Approach 3: Space-Optimized Variant

<ApproachTag tag="simple" />

A third approach demonstrating an alternative technique for solving this problem. This implementation optimizes either time or space complexity compared to the previous approaches.

<ComplexityBadges time="O(n)" space="O(1)" timeColor="green" spaceColor="green" timeNote="Optimized complexity" spaceNote="Efficient space usage" />

### Pseudocode

```plaintext
function approach3():
    // Implementation approach goes here
```

### Solution Code

<Tabs>
  <TabItem label="Python">
    <Code code={pySpaceOptimized} lang="python" title="n_queens_ii_space_optimized.py" />
  </TabItem>
  <TabItem label="C++">
    <Code code={cppSpaceOptimized} lang="cpp" title="n_queens_ii_space_optimized.cpp" />
  </TabItem>
  <TabItem label="Java">
    <Code code={javaSpaceOptimized} lang="java" title="NQueensIiSpaceOptimized.java" />
  </TabItem>
  <TabItem label="JavaScript">
    <Code code={jsSpaceOptimized} lang="javascript" title="n_queens_ii_space_optimized.js" />
  </TabItem>
  <TabItem label="Rust">
    <Code code={rustSpaceOptimized} lang="rust" title="n_queens_ii_space_optimized.rs" />
  </TabItem>
  <TabItem label="Go">
    <Code code={goSpaceOptimized} lang="go" title="n_queens_ii_space_optimized.go" />
  </TabItem>
</Tabs>


## Common Mistakes

<Aside type="caution" title="Watch for these pitfalls">
- **Diagonal formula:** Remember `abs(r1 - r2) == abs(c1 - c2)` detects both diagonals. In optimized approach, use `row - col` and `row + col`.
- **Forgetting to backtrack:** Always reset state (remove from sets or restore board) after recursion.
- **Checking only previous rows:** Since you place one queen per row from top to bottom, only check rows above.
- **Off-by-one in recursion:** Base case is `row == n`, not `row == n - 1`.
</Aside>

## Interview Tips

<Aside type="tip" title="Key trade-offs to discuss">
- **Array vs. Sets:** Array is simpler but requires O(N) checking per placement. Sets are O(1) but require more bookkeeping.
- **Why place one queen per row?** Reduces search space from O(N^N) to O(N!).
- **Why backtrack?** Pruning invalid branches early is essential for n > 4.
- **Follow-up:** Can you generate all solutions (NQueens I) instead of counting? Yes—just build the board configuration.
</Aside>

<PrevNextNav
  prevProblem={undefined}
  nextProblem={undefined}
/>
