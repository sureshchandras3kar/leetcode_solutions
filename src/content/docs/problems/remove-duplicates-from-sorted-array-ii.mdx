---
title: Remove Duplicates from Sorted Array II
description: Modify an array in-place to allow duplicates to appear at most twice. Two approaches — Two Pointers with Counter O(n) and Simple Two Pointers O(n) — in six languages.
leetcodeNumber: 80
difficulty: Medium
leetcodeSlug: remove-duplicates-from-sorted-array-ii
tags: [Array, Two Pointers]
prev:
  label: "#79 Word Search"
  link: "../word-search/"
next:
  label: "#82 Remove Duplicates from Sorted List II"
  link: "../remove-duplicates-from-sorted-list-ii/"
---

import { Aside, Tabs, TabItem, Code } from '@astrojs/starlight/components';
import ComplexityBadges from '../../../components/ComplexityBadges.astro';
import ApproachTag from '../../../components/ApproachTag.astro';
import LogicPlayer from '../../../components/LogicPlayer.astro';
import RelatedProblems from '../../../components/RelatedProblems.astro';
import PrevNextNav from '../../../components/PrevNextNav.astro';

import pyCounter    from '@solutions/Python/remove_duplicates_from_sorted_array_ii_counter.py?raw';
import cppCounter   from '@solutions/C++/remove_duplicates_from_sorted_array_ii_counter.cpp?raw';
import javaCounter  from '@solutions/Java/remove_duplicates_from_sorted_array_ii_counter.java?raw';
import jsCounter    from '@solutions/JavaScript/remove_duplicates_from_sorted_array_ii_counter.js?raw';
import rustCounter  from '@solutions/Rust/remove_duplicates_from_sorted_array_ii_counter.rs?raw';
import goCounter    from '@solutions/Golang/remove_duplicates_from_sorted_array_ii_counter.go?raw';
import pyoptimized from '@solutions/Python/remove_duplicates_from_sorted_array_ii_optimized.py?raw';
import cppoptimized from '@solutions/C++/remove_duplicates_from_sorted_array_ii_optimized.cpp?raw';
import javaoptimized from '@solutions/Java/RemoveDuplicatesFromSortedArrayIiOptimized.java?raw';
import jsoptimized from '@solutions/JavaScript/remove_duplicates_from_sorted_array_ii_optimized.js?raw';
import rustoptimized from '@solutions/Rust/remove_duplicates_from_sorted_array_ii_optimized.rs?raw';
import gooptimized from '@solutions/Golang/remove_duplicates_from_sorted_array_ii_optimized.go?raw';

import pySimple     from '@solutions/Python/remove_duplicates_from_sorted_array_ii_simple.py?raw';
import cppSimple    from '@solutions/C++/remove_duplicates_from_sorted_array_ii_simple.cpp?raw';
import javaSimple   from '@solutions/Java/remove_duplicates_from_sorted_array_ii_simple.java?raw';
import jsSimple     from '@solutions/JavaScript/remove_duplicates_from_sorted_array_ii_simple.js?raw';
import rustSimple   from '@solutions/Rust/remove_duplicates_from_sorted_array_ii_simple.rs?raw';
import goSimple     from '@solutions/Golang/remove_duplicates_from_sorted_array_ii_simple.go?raw';

## Problem Statement

Given an integer array `nums` sorted in **non-decreasing order**, modify it **in-place** such that each unique element appears **at most twice**. The **relative order** of the elements should be kept the same. Then return the number of elements `k`.

Consider the number of elements of `nums` to be `k`. To get accepted, you need to do the following things:
1. Change the array `nums` such that the first `k` elements of `nums` contain the elements which appear at most twice.
2. The remaining elements of `nums` are not important as well as the size of `nums`.
3. Return `k`.

<Aside type="tip" title="What to notice first">
- The array is **sorted**, which means duplicates are always consecutive.
- Each element can appear **at most 2 times**, not removed entirely like in Problem 26.
- You must modify the array **in-place** without using extra space for a new array.
- Only the first `k` elements matter; the rest can contain anything.
- You return `k`, not the modified array.
</Aside>

### Examples

| Input | Output | Explanation |
| --- | --- | --- |
| `[1, 1, 1, 2, 2, 3]` | `5` | Array becomes `[1, 1, 2, 2, 3, _]`. First 5 elements allow duplicates up to 2. |
| `[0, 0, 1, 1, 1, 1, 2, 3, 3]` | `7` | Array becomes `[0, 0, 1, 1, 2, 3, 3, _, _]`. Each unique value appears at most twice. |

## Constraints

- `1 <= nums.length <= 3 * 10^4`
- `-10^4 <= nums[i] <= 10^4`
- `nums` is sorted in **non-decreasing order**.

## Real-World Applications

<Aside type="note" title="Where this pattern appears">
- **Rate limiting** — Allow each user to perform an action at most twice within a time window, in a pre-sorted log.
- **Inventory management** — Keep at most 2 units of duplicate items in a sorted warehouse manifest.
- **Duplicate detection with tolerance** — Allow up to 2 occurrences of sensor readings before flagging an anomaly in ordered data.
- **Event deduplication** — Process duplicate events but cap their count at 2 for cost reduction in stream processing.
</Aside>

## Complexity Comparison

| Approach | Time | Space | Modifies Array | Best When |
| --- | --- | --- | --- | --- |
| Two Pointers with Counter | O(n) | O(1) | Yes | Tracking duplicate count explicitly — most intuitive |
| Simple Two Pointers | O(n) | O(1) | Yes | Compact solution — no explicit counter variable |

## Which approach should you learn first?

- Pick **Two Pointers with Counter** if you want explicit, easy-to-understand logic.
- Pick **Simple Two Pointers** if you prefer compact, clever indexing without a counter.

<div class="lc-approach-quick">
  <div class="lc-approach-quick__card lc-approach-quick__card--best">
    <div class="lc-approach-quick__label">Intuitive</div>
    <div class="lc-approach-quick__name">Two Pointers with Counter</div>
    <div class="lc-approach-quick__stats">O(n) time &middot; O(1) space</div>
  </div>
  <div class="lc-approach-quick__card">
    <div class="lc-approach-quick__label">Compact</div>
    <div class="lc-approach-quick__name">Simple Two Pointers</div>
    <div class="lc-approach-quick__stats">O(n) time &middot; O(1) space</div>
  </div>
</div>

## Approach 1: Two Pointers with Counter

<ApproachTag tag="recommended" />

Use two pointers and track the count of consecutive equal elements. Write an element to the result position if:
1. It's the first element, **or**
2. It's different from the previous element, **or**
3. It's the same as the previous but we've only seen it once so far (count < 2)

The counter resets when we encounter a new element.

<ComplexityBadges time="O(n)" space="O(1)" timeColor="green" spaceColor="green" timeNote="Single pass" spaceNote="No extra space" />

### Step-by-step Example

**Input:** `nums = [1, 1, 1, 2, 2, 3]`

| Step | i | nums[i] | count | Same as prev? | count < 2? | Action | k | Array State |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Start | - | - | - | - | - | Initialize | 0 | `[1, 1, 1, 2, 2, 3]` |
| 1 | 0 | 1 | 1 | - | - | First element, write | 1 | `[1, 1, 1, 2, 2, 3]` |
| 2 | 1 | 1 | 2 | Yes | Yes | Write (count < 2) | 2 | `[1, 1, 1, 2, 2, 3]` |
| 3 | 2 | 1 | 2 | Yes | No | Skip (count = 2) | 2 | `[1, 1, 1, 2, 2, 3]` |
| 4 | 3 | 2 | 1 | No | - | New element, write | 3 | `[1, 1, 2, 2, 2, 3]` |
| 5 | 4 | 2 | 2 | Yes | Yes | Write (count < 2) | 4 | `[1, 1, 2, 2, 2, 3]` |
| 6 | 5 | 3 | 1 | No | - | New element, write | 5 | `[1, 1, 2, 2, 3, 3]` |

Return `k = 5`, array first 5 elements: `[1, 1, 2, 2, 3]` ✓

**Input:** `nums = [0, 0, 1, 1, 1, 1, 2, 3, 3]`

| Step | i | nums[i] | Action | k | Progress |
| --- | --- | --- | --- | --- | --- |
| Start | - | - | Initialize | 0 | Start with k=0 |
| 1 | 0 | 0 | First element, write, count=1 | 1 | `[0, ...]` |
| 2 | 1 | 0 | Same, count < 2, write, count=2 | 2 | `[0, 0, ...]` |
| 3 | 2 | 1 | New element, write, count=1 | 3 | `[0, 0, 1, ...]` |
| 4 | 3 | 1 | Same, count < 2, write, count=2 | 4 | `[0, 0, 1, 1, ...]` |
| 5 | 4 | 1 | Same, count = 2, skip | 4 | No change |
| 6 | 5 | 1 | Same, count = 2, skip | 4 | No change |
| 7 | 6 | 2 | New element, write, count=1 | 5 | `[0, 0, 1, 1, 2, ...]` |
| 8 | 7 | 3 | New element, write, count=1 | 6 | `[0, 0, 1, 1, 2, 3, ...]` |
| 9 | 8 | 3 | Same, count < 2, write, count=2 | 7 | `[0, 0, 1, 1, 2, 3, 3, ...]` |

Return `k = 7` ✓

<LogicPlayer
  title="How the counter approach handles duplicates up to 2"
  description="Watch the counter track consecutive occurrences. When count reaches 2, we skip further duplicates. A new element resets the counter."
  steps={[
    {
      title: 'Start with first element',
      note: 'The first element is always written. We initialize count=1 for that element.',
      formula: 'k = 1, count = 1 (first occurrence)',
      callout: 'The first element is guaranteed to be in the result.',
      items: [
        { label: '1', state: 'match', index: 0 },
        { label: '1', state: 'default', index: 1 },
        { label: '1', state: 'default', index: 2 },
        { label: '2', state: 'default', index: 3 },
        { label: '2', state: 'default', index: 4 },
        { label: '3', state: 'default', index: 5 },
      ],
      memory: ['k = 1 (write position)', 'count = 1 (first 1)'],
    },
    {
      title: 'Second element matches first',
      note: 'nums[1] == nums[0], so this is a duplicate. count becomes 2. Since count < 2 now is false (it equals 2), we write it and increment k.',
      formula: 'nums[1] == nums[0]? Yes, count < 2? Yes, write',
      callout: 'Write the second occurrence and advance k.',
      items: [
        { label: '1', state: 'match', index: 0 },
        { label: '1', state: 'match', index: 1 },
        { label: '1', state: 'default', index: 2 },
        { label: '2', state: 'default', index: 3 },
        { label: '2', state: 'default', index: 4 },
        { label: '3', state: 'default', index: 5 },
      ],
      memory: ['k = 2 (advanced)', 'count = 2 (two 1s written)'],
    },
    {
      title: 'Third element matches second (but count = 2)',
      note: 'nums[2] == nums[1], but count is already 2. We skip this element and do not advance k.',
      formula: 'nums[2] == nums[1]? Yes, count < 2? No, skip',
      callout: 'Skip because we already wrote 2 occurrences of 1.',
      items: [
        { label: '1', state: 'match', index: 0 },
        { label: '1', state: 'match', index: 1 },
        { label: '1', state: 'active', index: 2 },
        { label: '2', state: 'default', index: 3 },
        { label: '2', state: 'default', index: 4 },
        { label: '3', state: 'default', index: 5 },
      ],
      memory: ['k = 2 (unchanged)', 'count = 2 (still)', 'Element 1 exhausted'],
    },
  ]}
/>

### Pseudocode

```plaintext
function removeDuplicates(nums):
    if nums is empty:
        return 0
    
    k = 1
    count = 1
    for i from 1 to len(nums) - 1:
        if nums[i] != nums[i - 1]:
            count = 1
            nums[k] = nums[i]
            k = k + 1
        else if count < 2:
            count = count + 1
            nums[k] = nums[i]
            k = k + 1
    
    return k
```

### Solution Code

<Tabs>
  <TabItem label="Python">
    <Code code={pyCounter} lang="python" title="remove_duplicates_from_sorted_array_ii_counter.py" />
  </TabItem>
  <TabItem label="C++">
    <Code code={cppCounter} lang="cpp" title="remove_duplicates_from_sorted_array_ii_counter.cpp" />
  </TabItem>
  <TabItem label="Java">
    <Code code={javaCounter} lang="java" title="remove_duplicates_from_sorted_array_ii_counter.java" />
  </TabItem>
  <TabItem label="JavaScript">
    <Code code={jsCounter} lang="javascript" title="remove_duplicates_from_sorted_array_ii_counter.js" />
  </TabItem>
  <TabItem label="Rust">
    <Code code={rustCounter} lang="rust" title="remove_duplicates_from_sorted_array_ii_counter.rs" />
  </TabItem>
  <TabItem label="Go">
    <Code code={goCounter} lang="go" title="remove_duplicates_from_sorted_array_ii_counter.go" />
  </TabItem>
</Tabs>

## Approach 2: Simple Two Pointers

<ApproachTag tag="simple" />

Instead of explicitly tracking a counter, use a clever indexing technique: only write an element if it differs from the element 2 positions before it (if it exists). This implicitly ensures at most 2 occurrences.

The key insight: `nums[k - 2]` is the element from 2 positions back in the result. If the current element is different from it, we can safely add it (ensuring at most 2 occurrences of any value).

<ComplexityBadges time="O(n)" space="O(1)" timeColor="green" spaceColor="green" timeNote="Single pass" spaceNote="No extra space" />

### Step-by-step Example

**Input:** `nums = [1, 1, 1, 2, 2, 3]`

| Step | k | nums[k-2] | nums[i] | Different? | Action | Result |
| --- | --- | --- | --- | --- | --- | --- |
| Start | 0 | - | - | - | Initialize | - |
| 1 | 1 | - | 1 | Always | Write first element | `[1, ...]` |
| 2 | 2 | - | 1 | Always | Write second element | `[1, 1, ...]` |
| 3 | 2 | 1 | 1 | No | Skip (already 2) | No change |
| 4 | 3 | 1 | 2 | Yes | Write | `[1, 1, 2, ...]` |
| 5 | 4 | 1 | 2 | Yes | Write | `[1, 1, 2, 2, ...]` |
| 6 | 5 | 2 | 3 | Yes | Write | `[1, 1, 2, 2, 3, ...]` |

Return `k = 5` ✓

### Pseudocode

```plaintext
function removeDuplicates(nums):
    if nums is empty:
        return 0
    
    k = 0
    for i from 0 to len(nums) - 1:
        if k < 2 or nums[i] != nums[k - 2]:
            nums[k] = nums[i]
            k = k + 1
    
    return k
```

The condition `k < 2 or nums[i] != nums[k - 2]` means:
- Always write the first 2 elements (`k < 2`)
- For elements beyond that, write only if they differ from the element 2 positions back (`nums[i] != nums[k - 2]`)

### Solution Code

<Tabs>
  <TabItem label="Python">
    <Code code={pySimple} lang="python" title="remove_duplicates_from_sorted_array_ii_simple.py" />
  </TabItem>
  <TabItem label="C++">
    <Code code={cppSimple} lang="cpp" title="remove_duplicates_from_sorted_array_ii_simple.cpp" />
  </TabItem>
  <TabItem label="Java">
    <Code code={javaSimple} lang="java" title="remove_duplicates_from_sorted_array_ii_simple.java" />
  </TabItem>
  <TabItem label="JavaScript">
    <Code code={jsSimple} lang="javascript" title="remove_duplicates_from_sorted_array_ii_simple.js" />
  </TabItem>
  <TabItem label="Rust">
    <Code code={rustSimple} lang="rust" title="remove_duplicates_from_sorted_array_ii_simple.rs" />
  </TabItem>
  <TabItem label="Go">
    <Code code={goSimple} lang="go" title="remove_duplicates_from_sorted_array_ii_simple.go" />
  </TabItem>
</Tabs>

## Approach 3: Optimized Two-Pointer

<ApproachTag tag="simple" />

A third approach demonstrating an alternative technique for solving this problem. This implementation optimizes either time or space complexity compared to the previous approaches.

<ComplexityBadges time="O(n)" space="O(1)" timeColor="green" spaceColor="green" timeNote="Optimized complexity" spaceNote="Efficient space usage" />

### Pseudocode

```plaintext
function approach3():
    // Implementation approach goes here
```

### Solution Code

<Tabs>
  <TabItem label="Python">
    <Code code={pyoptimized} lang="python" title="remove_duplicates_from_sorted_array_ii_optimized.py" />
  </TabItem>
  <TabItem label="C++">
    <Code code={cppoptimized} lang="cpp" title="remove_duplicates_from_sorted_array_ii_optimized.cpp" />
  </TabItem>
  <TabItem label="Java">
    <Code code={javaoptimized} lang="java" title="RemoveDuplicatesFromSortedArrayIiOptimized.java" />
  </TabItem>
  <TabItem label="JavaScript">
    <Code code={jsoptimized} lang="javascript" title="remove_duplicates_from_sorted_array_ii_optimized.js" />
  </TabItem>
  <TabItem label="Rust">
    <Code code={rustoptimized} lang="rust" title="remove_duplicates_from_sorted_array_ii_optimized.rs" />
  </TabItem>
  <TabItem label="Go">
    <Code code={gooptimized} lang="go" title="remove_duplicates_from_sorted_array_ii_optimized.go" />
  </TabItem>
</Tabs>


## Common Mistakes

<Aside type="caution" title="Watch for these pitfalls">
- **Confusing with Problem 26.** Problem 26 removes all duplicates. Problem 80 allows up to 2 occurrences. Don't mix them up!
- **Off-by-one errors in simple approach.** When checking `nums[k - 2]`, ensure `k >= 2` first, or use `k < 2 or` to guard the check.
- **Not resetting the counter.** In the counter approach, reset `count = 1` whenever you encounter a new element, not just incrementing it.
- **Modifying the comparison order.** Always compare with the previous element (or the element 2 back for the simple approach), not arbitrary indices.
- **Forgetting the sorted property.** The array is sorted, so duplicates are always consecutive. This is critical to both solutions.
</Aside>

<RelatedProblems problems={[
  { number: 26, title: 'Remove Duplicates from Sorted Array', difficulty: 'Easy', leetcodeSlug: 'remove-duplicates-from-sorted-array' },
  { number: 27, title: 'Remove Element', difficulty: 'Easy', leetcodeSlug: 'remove-element' },
  { number: 81, title: 'Search in Rotated Sorted Array II', difficulty: 'Medium', leetcodeSlug: 'search-in-rotated-sorted-array-ii' },
  { number: 283, title: 'Move Zeroes', difficulty: 'Easy', leetcodeSlug: 'move-zeroes' },
]} />

## Interview Tips

<Aside type="tip" title="Key trade-offs to discuss">
- **Why two pointers?** Because the array is already sorted. Duplicates are consecutive, so we can make decisions on-the-fly without extra space.
- **Counter vs. Index trick:** The counter approach is explicit and easier to explain. The index trick (comparing with `k - 2`) is more elegant but requires clearer thinking. Practice both.
- **Generalization:** If the problem asked for "at most k occurrences," you'd need the counter approach and adjust the limit.
- **Edge cases:** Single element (return 1), two elements (return 2), all same (return 2), all unique (return n).
- **Follow-ups to expect:** 
  - "What if duplicates can appear up to k times?" (Generalize the counter approach)
  - "Can you do this recursively?" (You can, but iteration is cleaner)
  - "What if the array is unsorted?" (You'd need a hash map — O(n) space)
</Aside>

<PrevNextNav
  prevProblem={{ number: 79, title: "Word Search", slug: "word-search" }}
  nextProblem={{ number: 81, title: "Search in Rotated Sorted Array II", slug: "search-in-rotated-sorted-array-ii" }}
/>
