---
title: Two Sum II – Input Array Is Sorted
description: Find two numbers in a sorted array that add to a target using two pointers in O(n) time and O(1) space — in six languages.
leetcodeNumber: 167
difficulty: Medium
leetcodeSlug: two-sum-ii-input-array-is-sorted
tags: [Array, Two Pointers, Binary Search]
prev:
  label: "#162 Find Peak Element"
  link: "../find-peak-element/"
next:
  label: "#169 Majority Element"
  link: "../majority-element/"
---

import { Aside, Tabs, TabItem, Code } from '@astrojs/starlight/components';
import ComplexityBadges from '../../../components/ComplexityBadges.astro';
import ApproachTag from '../../../components/ApproachTag.astro';
import LogicPlayer from '../../../components/LogicPlayer.astro';
import RelatedProblems from '../../../components/RelatedProblems.astro';
import PrevNextNav from '../../../components/PrevNextNav.astro';

import pyTwoPtr   from '@solutions/Python/two_sum_ii_two_pointer.py?raw';
import cppTwoPtr  from '@solutions/C++/two_sum_ii_two_pointer.cpp?raw';
import javaTwoPtr from '@solutions/Java/two_sum_ii_two_pointer.java?raw';
import jsTwoPtr   from '@solutions/JavaScript/two_sum_ii_two_pointer.js?raw';
import rustTwoPtr from '@solutions/Rust/two_sum_ii_two_pointer.rs?raw';
import goTwoPtr   from '@solutions/Golang/two_sum_ii_two_pointer.go?raw';

## Problem Statement

Given a **1-indexed** array of integers `numbers` that is already sorted in non-decreasing order, find two numbers such that they add up to a specific `target` number. Return the indices of the two numbers, `index1` and `index2`, added by one as an integer array `[index1, index2]` of length 2.

The tests are generated such that there is exactly one solution. You may not use the same element twice. Your solution must use only constant extra space.

<Aside type="tip" title="What to notice first">
- The array is already sorted — this is the key constraint that enables two pointers.
- Two pointers works because: if the sum is too small → move left pointer right to increase the sum; if too large → move right pointer left to decrease it.
- The answer is 1-indexed — return `index + 1` for each pointer position.
</Aside>

### Examples

| Input | Target | Output |
| --- | --- | --- |
| `[2, 7, 11, 15]` | `9` | `[1, 2]` |
| `[2, 3, 4]` | `6` | `[1, 3]` |
| `[-1, 0]` | `-1` | `[1, 2]` |

## Constraints

- `2 <= numbers.length <= 3 × 10^4`
- `-1000 <= numbers[i] <= 1000`
- `numbers` is sorted in non-decreasing order
- `-1000 <= target <= 1000`
- Exactly one valid answer exists

## Real-World Applications

<Aside type="note" title="Where this pattern appears">
- **Sorted event log pairing** — find two events in a time-sorted log whose durations sum to a target
- **Inventory matching** — find two sorted items whose combined weight exactly fills a container
- **Financial pairs** — match buy/sell orders in a sorted order book that net to zero
</Aside>

## Complexity Comparison

| Approach | Time | Space | Notes |
| --- | --- | --- | --- |
| Two Pointers | O(n) | O(1) | Optimal solution, requires sorted array |

\* n = length of the input array

## Approach: Two Pointers (Recommended)

<ApproachTag tag="recommended" />

Place one pointer at the start of the array and one at the end. Compute the sum of the two pointed-at values. If the sum equals the target, return the 1-indexed positions. If the sum is less than the target, advance the left pointer to increase the sum. If the sum is greater, retreat the right pointer to decrease it.

This works because the array is sorted: moving left right can only increase the sum; moving right left can only decrease it. Every step eliminates at least one element from consideration.

<ComplexityBadges time="O(n)" space="O(1)" timeColor="green" spaceColor="green" timeNote="At most one pass" spaceNote="Only two pointer variables" />

### Step-by-step Example

**Input:** `numbers = [2, 7, 11, 15]`, `target = 9`

| Step | left | right | sum | action |
| --- | --- | --- | --- | --- |
| 1 | 0 (2) | 3 (15) | 17 | > 9 → `right--` |
| 2 | 0 (2) | 2 (11) | 13 | > 9 → `right--` |
| 3 | 0 (2) | 1 (7) | 9 | == 9 → return `[1, 2]` ✓ |

<LogicPlayer
  title="Two pointers finding the pair in [2, 7, 11, 15] with target 9"
  description="Use Next or Autoplay to see how the left and right pointers move based on the sum comparison."
  target="9"
  steps={[
    {
      title: 'Initialize pointers at both ends',
      note: 'left = 0 points at 2, right = 3 points at 15. Sum = 17.',
      formula: 'sum = numbers[0] + numbers[3] = 2 + 15 = 17',
      callout: '17 > 9, so we need a smaller sum. Move right pointer left.',
      items: [
        { label: '2',  state: 'active', index: 0 },
        { label: '7',  state: 'default', index: 1 },
        { label: '11', state: 'default', index: 2 },
        { label: '15', state: 'active', index: 3 },
      ],
      memory: ['left = 0 (value 2)', 'right = 3 (value 15)', 'sum = 17 > 9'],
    },
    {
      title: 'Sum too large — move right pointer left',
      note: 'right-- → right = 2, now pointing at 11. Sum = 2 + 11 = 13.',
      formula: 'sum = numbers[0] + numbers[2] = 2 + 11 = 13',
      callout: 'Still > 9. Move right pointer left again.',
      items: [
        { label: '2',  state: 'active', index: 0 },
        { label: '7',  state: 'default', index: 1 },
        { label: '11', state: 'active', index: 2 },
        { label: '15', state: 'skipped', index: 3 },
      ],
      memory: ['left = 0 (value 2)', 'right = 2 (value 11)', 'sum = 13 > 9'],
    },
    {
      title: 'Sum still too large — move right pointer left again',
      note: 'right-- → right = 1, now pointing at 7. Sum = 2 + 7 = 9.',
      formula: 'sum = numbers[0] + numbers[1] = 2 + 7 = 9',
      callout: 'Sum equals target! Return 1-indexed positions [1, 2].',
      items: [
        { label: '2',  state: 'match', index: 0 },
        { label: '7',  state: 'match', index: 1 },
        { label: '11', state: 'skipped', index: 2 },
        { label: '15', state: 'skipped', index: 3 },
      ],
      memory: ['left = 0 (value 2)', 'right = 1 (value 7)', 'sum = 9 == 9', 'return [1, 2]'],
    },
  ]}
/>

### Pseudocode

```plaintext
function twoSum(numbers, target):
    left, right = 0, len(numbers) - 1
    while left < right:
        s = numbers[left] + numbers[right]
        if s == target:
            return [left + 1, right + 1]
        elif s < target:
            left++
        else:
            right--
```

### Solution Code

<Tabs>
  <TabItem label="Python">
    <Code code={pyTwoPtr} lang="python" title="two_sum_ii_two_pointer.py" />
  </TabItem>
  <TabItem label="C++">
    <Code code={cppTwoPtr} lang="cpp" title="two_sum_ii_two_pointer.cpp" />
  </TabItem>
  <TabItem label="Java">
    <Code code={javaTwoPtr} lang="java" title="two_sum_ii_two_pointer.java" />
  </TabItem>
  <TabItem label="JavaScript">
    <Code code={jsTwoPtr} lang="javascript" title="two_sum_ii_two_pointer.js" />
  </TabItem>
  <TabItem label="Rust">
    <Code code={rustTwoPtr} lang="rust" title="two_sum_ii_two_pointer.rs" />
  </TabItem>
  <TabItem label="Go">
    <Code code={goTwoPtr} lang="go" title="two_sum_ii_two_pointer.go" />
  </TabItem>
</Tabs>

## Common mistakes

<Aside type="caution" title="Watch for these pitfalls">
- Returning 0-indexed instead of 1-indexed results — the problem is explicitly 1-indexed.
- Not using the sorted property — a hash map would also work but wastes the O(1) space guarantee.
</Aside>

<RelatedProblems problems={[
  { number: 1,   title: 'Two Sum',           difficulty: 'Easy',   slug: 'problems/two-sum',           leetcodeSlug: 'two-sum' },
  { number: 15,  title: '3Sum',              difficulty: 'Medium', leetcodeSlug: 'three-sum' },
  { number: 125, title: 'Valid Palindrome',  difficulty: 'Easy',   slug: 'problems/valid-palindrome',  leetcodeSlug: 'valid-palindrome' },
]} />

## Interview Tips

<Aside type="tip" title="Key points to discuss">
- The sorted constraint is explicit in the problem name — always mention it as the reason two pointers work.
- Compare to Two Sum (#1): same problem but sorted input enables O(n) time with O(1) space instead of requiring an O(n) space hash map.
- Follow-up: "What if the array were not sorted?" → Fall back to the hash map approach from Two Sum #1.
- Follow-up: "What about binary search?" → Binary search per element gives O(n log n) which is worse than two pointers O(n).


<PrevNextNav
  prevProblem={{ number: 162, title: "Find Peak Element", slug: "find-peak-element" }}
  nextProblem={{ number: 169, title: "Majority Element", slug: "majority-element" }}
/></Aside>
