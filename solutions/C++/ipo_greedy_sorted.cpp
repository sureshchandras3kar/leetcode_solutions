#include <iostream>
#include <vector>
#include <unordered_map>
using namespace std;

class Solution {
public:
    int ipo_greedy_sorted(vector<int>& nums) {
        // Ipo - Greedy Sorted approach
        return 0;
    }
};

int main() {
    return 0;
}
