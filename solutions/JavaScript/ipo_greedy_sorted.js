/**
 * {@param {number[]} nums}
 * {@return {number}}
 */
var ipoGreedysorted = function(nums) {
    // Ipo - Greedy Sorted approach
    return 0;
};

if (require.main === module) {
    // Test cases
}
